electron + express server + create-react-app in single repo

## Start Electron window

$ npm run electron

## Start Server only

$ cd server && npm start

## Start frontend only

$ cd web && npm start
